# Local Agent Preferences

- Always include local file links in responses when referring to changed files, generated SQL, or places the user may want to inspect.
- Prefer clickable links with absolute local paths when available.
- When the user asks for a local link to test, prefer the running app URL such as `http://localhost:3000` over only file links.
